import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'club.grizzlys.rudelcheck',
  appName: 'RudelCheck',
  webDir: 'dist-app'
};

export default config;
